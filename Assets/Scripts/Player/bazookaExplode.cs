using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class bazookaExplode : MonoBehaviour 
{
    // no voy a usar este script


    /* Store objects that have already collided
    private HashSet<GameObject> collidedObjects = new HashSet<GameObject>();

    void OnTriggerEnter(Collider other)
    {
        // Check if the object has already collided
        if (!collidedObjects.Contains(other.gameObject))
        {
            // Process the collision
            Debug.Log($"Explosion collided with: {other.gameObject.name}");

            // Add the object to the set
            collidedObjects.Add(other.gameObject);

            // Add any other logic for the collision here (e.g., applying damage)
        }
    }*/
}
